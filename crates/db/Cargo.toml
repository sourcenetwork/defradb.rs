[package]
name = "db"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "DefraDB database and collection management"

[package.metadata.cargo-machete]
ignored = ["anyhow", "bytes", "libp2p", "multihash", "p2p", "rand", "serde", "serde_bytes", "serde_cbor", "zeroize"]

[features]
default = ["p2p", "native", "wasmtime-runtime", "wasm-rules"]
p2p = ["dep:libp2p", "p2p/libp2p-transport"]
native = ["dep:tokio", "events/channel", "acp/native"]
wasmtime-runtime = ["lens/wasmtime-runtime"]
# Merge rules carried as wasm modules named by CID, run under a fuel budget.
# Native only: the browser build judges nothing this way.
wasm-rules = ["dep:wasmtime"]

[lib]
bench = false

[dependencies]
web-time.workspace = true
# Async runtime
async-trait.workspace = true
async-lock.workspace = true
n0-future.workspace = true
futures.workspace = true

# Serialization
serde.workspace = true
serde_json.workspace = true
serde_bytes.workspace = true
ciborium.workspace = true
chrono = { version = "0.4", features = ["serde"] }

# Bytes (zero-copy block data on hot path)
bytes.workspace = true

# Memory zeroization for sensitive key material
zeroize = "1"

# Error handling
anyhow.workspace = true
thiserror.workspace = true

# Logging
tracing.workspace = true

# IPLD
cid.workspace = true
multihash.workspace = true

# Hashing
sha2.workspace = true
rapidhash.workspace = true

# Lock-free shared state
kovan.workspace = true
kovan-map.workspace = true
kovan-queue.workspace = true

# Random number generation (for Counter CRDT nonces)
rand.workspace = true

# Encoding
base64.workspace = true
hex.workspace = true

libp2p = { workspace = true, optional = true }

# Native-only (optional)
tokio = { workspace = true, features = ["rt", "fs", "sync", "time"], optional = true }
parking_lot.workspace = true

reqwest = { version = "0.12", default-features = false, features = [
    "json",
    "charset",
    "http2",
    "system-proxy",
    "rustls-tls-native-roots",
] }

# Internal crates
storage = { path = "../storage" }
datastore = { path = "../datastore" }
schema = { path = "../schema" }
document = { path = "../document" }
query = { path = "../query" }
blockstore = { path = "../blockstore" }
crypto = { path = "../crypto" }
defra-core = { path = "../defra-core" }
kms = { path = "../kms" }
identity = { path = "../identity" }
acp = { path = "../acp", default-features = false }
crdt = { path = "../crdt" }
events = { path = "../events", default-features = false }
lens = { path = "../lens", default-features = false }
telemetry = { path = "../telemetry" }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
p2p = { path = "../p2p", default-features = false }
wasmtime = { version = "46.0.2", optional = true, default-features = false, features = [
    "runtime",
    "cranelift",
    "std",
] }

# A browser peer replicates over the relay-only iroh transport; libp2p has no
# browser build.
[target.'cfg(target_arch = "wasm32")'.dependencies]
p2p = { path = "../p2p", default-features = false, features = ["iroh-transport"] }
tokio = { workspace = true, features = ["sync"] }
wasm-bindgen-futures = "0.4"

[dev-dependencies]
tokio = { workspace = true, features = ["rt-multi-thread", "macros", "net", "sync", "time"] }
wat = "1"
axum.workspace = true
proptest.workspace = true
blst = "0.3"
getrandom = "0.2"
tempfile = "3"
ed25519-dalek.workspace = true

[lints]
workspace = true
