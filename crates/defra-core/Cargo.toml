[package]
name = "defra-core"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[package.metadata.cargo-machete]
ignored = ["tracing"]

[lib]
bench = false

[dependencies]
# Error handling
thiserror.workspace = true

# Serialization
serde.workspace = true
serde_json.workspace = true
serde_bytes.workspace = true
ciborium.workspace = true
uuid.workspace = true
multibase = "0.9"

# IPLD
cid.workspace = true
multihash.workspace = true
serde_ipld_dagcbor.workspace = true
ipld-core.workspace = true

# Hashing
rapidhash.workspace = true

# Lock-free shared state
kovan-map.workspace = true
kovan-queue.workspace = true

# Cryptography (for CID generation)
sha2.workspace = true
rand.workspace = true
zeroize = { version = "1.8", features = ["derive"] }

# Async
async-trait.workspace = true
bytes.workspace = true

# Logging
tracing.workspace = true

# Request-scoped ambient identity (task_local). Not needed on wasm, where the
# runtime is single-threaded and HTTP-server-side scoping does not apply.
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
tokio = { workspace = true, features = ["rt"] }

[target.'cfg(not(target_arch = "wasm32"))'.dev-dependencies]
tokio = { workspace = true, features = ["rt", "macros"] }

[dev-dependencies]
futures.workspace = true
multihash-codetable.workspace = true

[lints]
workspace = true
